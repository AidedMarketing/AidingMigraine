# Notification Server Environment Variables
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit the actual .env file to git!

# Server Configuration
PORT=3000
NODE_ENV=development

# VAPID Keys for Web Push (Generate with: npx web-push generate-vapid-keys)
VAPID_SUBJECT=mailto:your-email@example.com
VAPID_PUBLIC_KEY=your_vapid_public_key_here
VAPID_PRIVATE_KEY=your_vapid_private_key_here

# Admin API Key (Generate a secure random key)
# Used to protect admin endpoints like GET /api/subscriptions
# Example generation: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
ADMIN_API_KEY=generate_a_secure_random_key_here

# CORS Allowed Origins (comma-separated)
# Add your app's URLs here - must match exactly (including protocol and port)
ALLOWED_ORIGINS=http://localhost:8080,http://127.0.0.1:8080,https://your-domain.com

# Database Path (optional - defaults to subscriptions.json within data directory)
# Note: For security, only the filename is used, path traversal is prevented
# DB_PATH=subscriptions.json
